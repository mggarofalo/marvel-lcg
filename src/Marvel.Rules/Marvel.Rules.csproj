<Project Sdk="Microsoft.NET.Sdk">

  <!-- The fold: state, zones, phases, timing, prompts and semantic events.
       No cards, filesystem or network I/O, or UI framework references.
       See docs/presentation-layer.md, "Build boundary". -->
  <PropertyGroup>
    <RootNamespace>Marvel.Rules</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Marvel.Core/Marvel.Core.csproj" />
  </ItemGroup>

</Project>
