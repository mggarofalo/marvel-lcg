"""Cross-language test vectors for the RNG contract (`docs/rng-contract.md`).

The fixture this writes is the acceptance test for MARVEL-8: a C# generator
that reproduces every vector is compatible with the Python one, and one that
does not is not.
"""
